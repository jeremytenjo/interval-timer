import ListItem from '../../_common/ListItem'
import Add from '../../../../../../../../../icons/Add'

export default function InstallWebAppkUi({ onClick }) {
  return <ListItem label='Install' Icon={Add} onClick={onClick} />
}
