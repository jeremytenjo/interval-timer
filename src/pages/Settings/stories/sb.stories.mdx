import { Meta, Story, Canvas } from '@storybook/addon-docs'
import Settings from '..'

<Meta title='Pages/Settings/index' component={Settings} args={{ test: 'test' }} />

# Settings Page

export const Template = (args) => <Settings {...args} />

<Canvas>
  <Story name='default' argTypes={{}} args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
